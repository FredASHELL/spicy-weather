# spicy-weather
Fairly simple Python script to see the weather. [NEEDS PYTHON3 AND REQUESTS PACKAGE]
# warning
Needs macOS. App needs Automator. Needs requests package. To install requests on macOS, put in Terminal, pip install requests. Opens in Terminal. You may need to allow access for the .py file.
