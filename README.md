# spicy-weather
Fairly simple Python script to see the weather. [NEEDS PYTHON3 AND REQUESTS PACKAGE]
# warning
Needs requests package. To install requests on macOS, put in Terminal, pip install requests. On Windows and Linux, you're on your own. Opens in Terminal. On macOS, you may need to allow access for the .py file.
